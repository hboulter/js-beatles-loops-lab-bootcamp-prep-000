function theBeatlesPlay(musicians, instruments) {
  var x = [];
  for (var i = 0; i <= musicians.length; i++) {
    var y = musicians + instruments
    x.push(y)
  } return `${musicians} plays ${instruments}`
}
